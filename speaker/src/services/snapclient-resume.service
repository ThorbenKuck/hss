[Unit]
Description=Resume Snapclient Setup after Reboot
After=network.target sound.target

[Service]
Type=oneshot
ExecStart=/bin/bash $SCRIPT_PATH -y
RemainAfterExit=no

[Install]
WantedBy=multi-user.target
